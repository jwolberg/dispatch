// GENERATED by scripts/embed-templates.mjs — DO NOT EDIT.
//
// Source of truth: scripts/repo-ci/ and scripts/repo-skills/ (#4 AC 11).
// Regenerate with `npm run embed:templates`; `npm run verify` fails if this is stale.

/** Template file contents, keyed by path relative to `scripts/`. */
export const EMBEDDED_TEMPLATES: Readonly<Record<string, string>> = Object.freeze({
  "repo-ci/ci-node.yml": "# Dispatch CI gate — runs the repo's lint/test/build on every pull request so the\n# board's check-driven states reflect real results (Building → Ready to test →\n# Blocked). Each step runs only if the matching npm script exists, so this is\n# safe to drop into any Node/JS repo; adapt the steps for other stacks.\n#\n# NOTE: GitHub does not trigger workflows on events authored by GITHUB_TOKEN\n# (the github-actions[bot]). If claude-code-action opens PRs as the bot\n# (API-key-only setup), this gate will NOT run on those PRs. Install the Claude\n# GitHub App (/install-github-app) so PRs are app-authored and trigger CI.\nname: CI\non:\n  pull_request:\nconcurrency:\n  group: ci-${{ github.ref }}\n  cancel-in-progress: true\njobs:\n  test:\n    runs-on: ubuntu-latest\n    timeout-minutes: 15\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: \"20\"\n      - name: Install dependencies\n        run: npm ci || npm install\n      - name: Lint\n        run: npm run lint --if-present\n      - name: Test\n        run: npm run test --if-present\n      - name: Build\n        run: npm run build --if-present\n",
  "repo-ci/ci-python.yml": "# Dispatch CI gate (Python) — runs on every pull request so the board's\n# check-driven states reflect real results (Building → Ready to test → Blocked).\n# Safe-by-default: dependency installs run only when the matching file exists, and\n# lint/test run only when a linter/tests are actually present — so this is a no-op\n# on a bare repo but catches real failures once you add deps/tests.\n#\n# NOTE: GitHub does not trigger workflows on events authored by GITHUB_TOKEN\n# (github-actions[bot]). If claude-code-action opens PRs as the bot, install the\n# Claude GitHub App so PRs are app-authored and trigger this gate.\nname: CI\non:\n  pull_request:\nconcurrency:\n  group: ci-${{ github.ref }}\n  cancel-in-progress: true\njobs:\n  test:\n    runs-on: ubuntu-latest\n    timeout-minutes: 15\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-python@v5\n        with:\n          python-version: \"3.x\"\n      - name: Install dependencies\n        run: |\n          python -m pip install --upgrade pip\n          # Install only what's declared — a missing file is a no-op, but a broken\n          # requirements/pyproject legitimately fails CI.\n          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi\n          if [ -f requirements-dev.txt ]; then pip install -r requirements-dev.txt; fi\n          if [ -f pyproject.toml ] || [ -f setup.py ]; then pip install -e . || pip install .; fi\n      - name: Lint\n        run: |\n          # Runs only if a linter got installed (declared in your deps).\n          if command -v ruff >/dev/null 2>&1; then ruff check .;\n          elif command -v flake8 >/dev/null 2>&1; then flake8;\n          else echo \"no linter installed — skipping\"; fi\n      - name: Test\n        run: |\n          # Runs only if tests exist (avoids pytest's \"no tests collected\" failure).\n          if [ -d tests ] || ls test_*.py *_test.py >/dev/null 2>&1; then\n            pip install pytest >/dev/null 2>&1 || true\n            pytest\n          else\n            echo \"no tests found — skipping\"\n          fi\n",
  "repo-ci/claude.yml": "name: Claude Code\non:\n  issues:\n    types: [opened]\n  issue_comment:\n    types: [created]\njobs:\n  claude:\n    if: |\n      (github.event_name == 'issues' && contains(github.event.issue.body, '@claude')) ||\n      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude'))\n    runs-on: ubuntu-latest\n    timeout-minutes: 30\n    permissions:\n      # Push Claude's branch. The PR is opened by Dispatch, not here, so\n      # `pull-requests: write` is deliberately absent (ADR-0006 [2]).\n      contents: write\n      issues: write # the tracking comment claude-code-action posts\n      id-token: write\n      actions: read\n    steps:\n      - uses: actions/checkout@v4\n      - uses: anthropics/claude-code-action@v1\n        id: claude\n        with:\n          __CLAUDE_AUTH_INPUT__\n          # Pass the default GITHUB_TOKEN *explicitly* (ADR-0006 [2]). This input has\n          # no default: omit it and claude-code-action performs an App token exchange\n          # against Anthropic's Claude GitHub App, which most repos do not have\n          # installed, and 401s (observed — #25). `github.token` is all the branch\n          # push needs, and it writes no credential into the target repo.\n          github_token: ${{ github.token }}\n          # Once this deployment registers a GitHub App, Dispatch files issues with the\n          # App installation token, so the issue is authored by `<app-slug>[bot]`\n          # (type: Bot) rather than the human PAT. claude-code-action refuses a\n          # bot-initiated trigger unless the bot is allow-listed, so the build fails\n          # immediately (#29). This line allow-lists THIS deployment's own App bot,\n          # injected from the registered App's slug. PAT-only deployments file issues\n          # as a human actor, and Dispatch omits this line entirely.\n          __ALLOWED_BOTS_INPUT__\n          # Dispatch opens the PR with its App installation token, which triggers\n          # on: pull_request CI without an approval gate (observed, ADR-0006 [8]).\n          #\n          # Do NOT set `prompt:` here. A static prompt forces automation mode, which\n          # IGNORES the @claude comment. Omitting it enables interactive (mention)\n          # mode: Claude reads the comment, runs it (incl. project skills under\n          # .claude/skills/), posts a tracking comment, and pushes a branch.\n          claude_args: |\n            --append-system-prompt \"Dispatch installs its CI skills under .claude/skills/ namespaced as ci-plan, ci-implement, and ci-debug (kept distinct from any interactive plan/implement/debug skills the repo carries). When the @claude comment names one of them, run that ci-* skill. Implement the change on a branch and commit it; Dispatch opens the pull request from your branch. Reference the issue with 'Fixes #<n>'.\"\n",
  "repo-ci/deploy.yml": "# Dispatch deploy gate (OPTIONAL) — verify-before-production.\n#\n# On every merge to the default branch:\n#   1. staging    — deploy to the persistent GitHub \"staging\" Environment, then run\n#                   smoke / e2e tests against it.\n#   2. production — runs only AFTER staging succeeds AND a manual approval on the\n#                   GitHub \"production\" Environment (the 🔒 gate, diagram step 9).\n#\n# Opt-in + safe-by-default: every deploy/test step is `--if-present`, so this is a\n# no-op until the repo defines the matching npm scripts. Wire these to your host:\n#   deploy:staging · test:smoke (and/or test:e2e) · deploy:production\n# Adapt the run steps for non-Node stacks.\n#\n# Required one-time GitHub setup (Settings → Environments):\n#   - create the \"staging\" and \"production\" Environments\n#   - add Required reviewers to \"production\" to arm the manual approval gate\n#   (without the reviewers rule, production deploys immediately after staging.)\n#\n# NOTE: assumes the default branch is `main` — adjust `branches:` if not. Like\n# ci.yml, GitHub won't trigger this on merges authored by the default GITHUB_TOKEN\n# /bot; with the Claude GitHub App (PRs/merges are app-authored) it fires.\nname: Deploy\non:\n  push:\n    branches: [main]\n# Don't cancel an in-flight deploy — let staging/production finish.\nconcurrency:\n  group: deploy-${{ github.ref }}\n  cancel-in-progress: false\njobs:\n  staging:\n    runs-on: ubuntu-latest\n    timeout-minutes: 20\n    environment: staging\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: \"20\"\n      - name: Install dependencies\n        run: npm ci || npm install\n      - name: Build\n        run: npm run build --if-present\n      - name: Deploy to staging\n        run: npm run deploy:staging --if-present\n      - name: Smoke tests\n        run: npm run test:smoke --if-present\n      - name: E2E tests\n        run: npm run test:e2e --if-present\n\n  production:\n    # Gate: only after staging deploy + smoke/e2e tests pass, and only once the\n    # \"production\" Environment's required-reviewers approval is granted.\n    needs: staging\n    runs-on: ubuntu-latest\n    timeout-minutes: 20\n    environment: production\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: \"20\"\n      - name: Install dependencies\n        run: npm ci || npm install\n      - name: Build\n        run: npm run build --if-present\n      - name: Deploy to production\n        run: npm run deploy:production --if-present\n",
  "repo-ci/review.yml": "# Dispatch review gate — emit the code-review artifact Dispatch's Ship gate reads.\n#\n# On every pull request, run the ci-review skill and commit the artifact triple\n#   .TerMinal/reviews/<pr>/<short_sha>.md   (verdict + test_status frontmatter)\n#   .TerMinal/reviews/<pr>/findings.json    (per-finding state)\n#   .TerMinal/reviews/<pr>/suggestions.json (non-blocking notes)\n# into the PR branch, matching the code-review agent contract. Dispatch reads\n# these at the PR head and refuses Ship unless verdict=approve, test_status=pass,\n# and there are zero open findings at medium or above (T2-5 / #15).\n#\n# Anti-recursion: `paths-ignore` on `.TerMinal/reviews/**` means the artifact\n# commit this workflow pushes does NOT re-trigger a review, and the commit itself\n# carries `[skip ci]`. The credential is the same secret setup already installed\n# (CLAUDE_CODE_OAUTH_TOKEN or ANTHROPIC_API_KEY) — nothing new is written to the\n# repo.\nname: Dispatch Review\non:\n  pull_request:\n    types: [opened, synchronize, reopened]\n    paths-ignore:\n      - \".TerMinal/reviews/**\"\npermissions:\n  contents: write # commit the artifact back to the PR branch\n  pull-requests: read\nconcurrency:\n  # One review per PR; a new push cancels an in-flight review of the old commit.\n  group: dispatch-review-${{ github.event.pull_request.number }}\n  cancel-in-progress: true\njobs:\n  review:\n    runs-on: ubuntu-latest\n    timeout-minutes: 20\n    steps:\n      - uses: actions/checkout@v4\n        with:\n          # Check out the PR head branch (not the detached merge ref) so the\n          # artifact commit can be pushed back to it, with full history so the\n          # review can diff against the base.\n          ref: ${{ github.event.pull_request.head.ref }}\n          fetch-depth: 0\n      - uses: anthropics/claude-code-action@v1\n        id: review\n        with:\n          __CLAUDE_AUTH_INPUT__\n          # Explicit default token (ADR-0006 [2]) — all the artifact commit needs.\n          github_token: ${{ github.token }}\n          # A static prompt runs claude-code-action in automation mode: it executes\n          # the ci-review skill non-interactively rather than waiting for an\n          # @claude mention. The skill writes the artifact files; it does not open\n          # a PR (Dispatch does that) and does not merge.\n          claude_args: |\n            --append-system-prompt \"Run the ci-review skill in .claude/skills/ci-review to review pull request #${{ github.event.pull_request.number }} at head commit ${{ github.event.pull_request.head.sha }}. Write the review artifact triple under .TerMinal/reviews/${{ github.event.pull_request.number }}/ and nothing else. Do not open a pull request and do not merge.\"\n      - name: Commit the review artifact\n        run: |\n          git config user.name \"dispatch-review[bot]\"\n          git config user.email \"dispatch-review[bot]@users.noreply.github.com\"\n          if [ -n \"$(git status --porcelain .TerMinal/reviews)\" ]; then\n            git add .TerMinal/reviews\n            git commit -m \"review: artifact for ${{ github.event.pull_request.head.sha }} [skip ci]\"\n            git push origin \"HEAD:${{ github.event.pull_request.head.ref }}\"\n          else\n            echo \"ci-review produced no artifact changes to commit.\"\n          fi\n",
  "repo-skills/ci-debug/SKILL.md": "---\nname: ci-debug\ndescription: Reproduce the failure, find the root cause, and push a minimal fix to the open PR\n---\n\nYou are running in CI via `claude-code-action`, triggered by an `@claude` mention — usually on a pull/merge request whose checks are failing, or an issue describing a bug.\n\nGoal: get the change working by finding the root cause and pushing the smallest fix.\n\n## Task\n\n1. Restate the observed failure (failing checks, error output, or the described bug) and the expected behavior.\n2. Form 2–4 plausible hypotheses. Gather evidence from the failing execution path, logs, and tests — inspect the fewest files needed.\n3. Identify the most likely root cause and why it beats the alternatives.\n4. Apply the minimal fix and push it to the existing PR/MR branch.\n5. Verify by re-running the relevant tests and lint where possible.\n6. Post a short root-cause explanation as a comment on the PR/MR (or the issue).\n\n## Rules\n\n- Do not guess without evidence; prefer the smallest fix that explains the failure.\n- No unrelated refactors and no scope expansion.\n- Push the fix to the existing PR/MR branch — do not open a second one.\n- If you cannot fix it safely, comment the blocker clearly instead of forcing a change.\n",
  "repo-skills/ci-implement/SKILL.md": "---\nname: ci-implement\ndescription: Implement the issue and open a pull request that closes it on merge\n---\n\nYou are running in CI via `claude-code-action`, triggered by an `@claude` mention on a GitHub/GitLab issue.\n\nGoal: implement exactly what the issue asks and open a pull/merge request that references the issue so it auto-closes on merge.\n\n## Task\n\n1. Read the issue title and body to determine the scope.\n2. Inspect the relevant files before changing anything; follow the repo's conventions (read `CLAUDE.md` / `README` if present).\n3. Make the smallest correct change that satisfies the issue.\n4. Run the project's lint and tests if they exist; make sure the project still builds.\n5. Open a pull/merge request whose description includes a closing reference to the issue — `Fixes #<issue-number>` on GitHub, `Closes #<issue-number>` on GitLab — so it auto-closes on merge.\n\n## Rules\n\n- Implement ONLY what the issue describes — no scope creep, no unrelated refactors.\n- Reuse existing patterns and components; keep the change minimal and focused.\n- Prefer simple solutions over clever abstractions; do not add dependencies without need.\n- Always open the PR/MR — do not stop after editing files. The PR is the deliverable.\n- If the issue is ambiguous, make the smallest reasonable assumption, state it in the PR body, and proceed.\n",
  "repo-skills/ci-plan/SKILL.md": "---\nname: ci-plan\ndescription: Post a step-by-step implementation plan for this issue as a comment — no code changes, no PR\n---\n\nYou are running in CI via `claude-code-action`, triggered by an `@claude` mention on a GitHub/GitLab issue.\n\nGoal: produce a concrete, reviewable implementation plan for the issue and post it as a single comment. Do NOT write code or open a pull/merge request.\n\n## Task\n\n1. Read the issue title and body — that is the work to plan.\n2. Inspect the relevant files in the checked-out repo so the plan is grounded in the real codebase (read `CLAUDE.md` / `README` first if present).\n3. Post ONE comment on the issue containing:\n   - **Summary** — one sentence on the change.\n   - **Approach** — the strategy and any key decisions or assumptions.\n   - **Steps** — an ordered checkbox list of concrete steps.\n   - **Files to touch** — the real paths you expect to create or modify.\n   - **Risks** — edge cases, data migrations, or anything that could break.\n\n## Rules\n\n- Plan only what the issue asks; do not expand scope.\n- Prefer the smallest change that delivers the user value.\n- Reference actual files and paths from this repo — no placeholders.\n- Do NOT modify files or open a PR/MR. This step is plan-only; implementation happens later.\n",
  "repo-skills/ci-review/SKILL.md": "---\nname: ci-review\ndescription: Review this pull request and write the Dispatch review-artifact triple (verdict + findings + suggestions) — no PR, no merge\n---\n\nYou are running in CI via `claude-code-action`, triggered automatically on a pull request. Your job is to review the PR and write **three files** that Dispatch's Ship gate reads. Do NOT open a pull request, comment, or merge.\n\n## What the gate needs\n\nDispatch refuses Ship unless your artifact says **`verdict: approve`**, **`test_status: pass`**, and there are **zero open findings at `medium` or above**. A missing or malformed artifact blocks Ship (fail-closed), so the format below is exact — match it.\n\n## Steps\n\n1. **Run the tests.** Use the repo's own command (`npm test`, `pytest`, `cargo test`, …). Record the runner, command, exit code, and pass/fail/skip counts.\n2. **Review the diff** of this PR against its base. Treat it as if it ships to production: correctness, security, and architecture are first-class. Assign each real problem a `severity` of `critical`, `high`, `medium`, or `low`.\n3. **Decide the verdict** deterministically:\n   - `blocked` if `test_status` is not `pass`, or any `critical` finding exists.\n   - `request-changes` if there is any open `high` or `medium` finding.\n   - `approve` only when tests pass and there are no open findings at `medium` or above.\n4. **Write the three files** (create the directory). `<PR>` is this PR's number; `<SHORT_SHA>` is the first 7 chars of the head commit SHA.\n\n## Files to write\n\n`.TerMinal/reviews/<PR>/<SHORT_SHA>.md` — frontmatter is what the gate parses; keep these keys exactly:\n\n```\n---\npr: <owner>/<repo>#<PR>\ncommit: <full 40-char head sha>\nshort_sha: <7-char>\nkind: review\ngenerated: <ISO 8601 datetime, UTC>\ngenerator: claude-code-action:ci-review\nverdict: approve | request-changes | blocked\nsummary: \"<one line, <=120 chars — quote it>\"\ntest_status: pass | fail | partial | error | missing\ntest_runner: <e.g. vitest>\ntest_command: \"<exact command>\"\ntest_exit_code: <int>\n---\n\n## Summary\n<one paragraph: what changed and the headline judgment>\n\n## Findings\n<one section per finding, or \"No blocking findings.\">\n```\n\n`.TerMinal/reviews/<PR>/findings.json` — every finding (blocking severities); `[]` if none:\n\n```json\n{\n  \"pr\": \"<owner>/<repo>#<PR>\",\n  \"updated_at\": \"<ISO 8601>\",\n  \"findings\": [\n    {\n      \"id\": \"<8-char hex>\",\n      \"title\": \"<title>\",\n      \"severity\": \"critical|high|medium|low\",\n      \"category\": \"<category>\",\n      \"file\": \"<path:line>\",\n      \"confidence\": <1-10>,\n      \"first_seen_sha\": \"<7-char>\",\n      \"first_seen_at\": \"<ISO 8601>\",\n      \"status\": \"open\",\n      \"status_changed_at\": \"<ISO 8601>\",\n      \"status_changed_by\": \"auto:code-review\",\n      \"resolved_in_sha\": null\n    }\n  ]\n}\n```\n\n`.TerMinal/reviews/<PR>/suggestions.json` — non-blocking notes only (never a correctness/security/test failure — those are findings); `{\"pr\": \"...\",\"updated_at\":\"...\",\"suggestions\": []}` if none.\n\n## Rules\n\n- **`verdict` and `test_status` are load-bearing** — the gate reads them literally. Never write `approve` when tests failed or a `medium`+ finding is open.\n- Put only non-blocking polish in `suggestions.json`; anything that should block a merge is a finding.\n- Do not modify code, open a PR, or merge. Write only the three artifact files — the workflow commits them.\n- `id = sha256(title + \"|\" + file)` truncated to 8 hex chars, so a finding keeps its id across re-reviews.\n",
});
