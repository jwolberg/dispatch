import { Page, Placeholder } from "../components/Page.js";

// Built out in P1-T9 (discovery list, Track, repo cards).
export function ReposPage() {
  return (
    <Page title="Repos">
      <Placeholder note="Repo discovery and tracking — coming in P1-T9." />
    </Page>
  );
}
