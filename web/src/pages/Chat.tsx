import { Page, Placeholder } from "../components/Page.js";

// Built out in P2-T4/T5 (repo-scoped streaming spec chat + ticket preview).
export function ChatPage() {
  return (
    <Page title="Spec chat">
      <Placeholder note="Spec refinement chat — coming in P2." />
    </Page>
  );
}
