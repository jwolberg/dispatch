import { Page, Placeholder } from "../components/Page.js";

// Built out in P4-T5 (reverse-chronological activity feed).
export function ActivityPage() {
  return (
    <Page title="Activity">
      <Placeholder note="Activity feed — coming in P4." />
    </Page>
  );
}
