import { Page, Placeholder } from "../components/Page.js";

// Built out in P3-T6 (kanban columns derived from provider state).
export function BoardPage() {
  return (
    <Page title="Board">
      <Placeholder note="The board renders once tickets are filed (P3)." />
    </Page>
  );
}
